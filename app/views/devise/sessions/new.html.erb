<div id="devise">
	<div class="barra-superior-devise">
	<a href="/">
		<h1>SERIESCHUCHU</h1>
	</a>
	</div>
	<div class="contenido-devise">
	<h1>LOG IN</h1>

	<%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
	  <div class="devise-input">
	  <%= f.email_field :email, :placeholder=> "Email", :autofocus => true %></div>

	  <div class="devise-input">
	  <%= f.password_field :password, :placeholder=> "Password" %></div>

	  <% if devise_mapping.rememberable? -%>
	    <div><%= f.check_box :remember_me %> <%= f.label "Recuérdame" %></div>
	  <% end -%>

	  <div class="devise-boton-login"><%= f.submit "Entrar" %></div>
	<% end %>

	<%= render "devise/shared/links" %>
	</div>
</div>